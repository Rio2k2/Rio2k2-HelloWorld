public class Lab {
    /**
     * TODO: Change this method so that it returns "Hello, world!"
     *
     * This method currently contains the line:
     * return "Hello, world!";
     * That means that this method is a piece of code that produces a String "Hello, world!", 
     * and when this method is used in other parts of my program, the value "Hello, world!"
     * is returned.
     * 
     * Instead, this method must return a hello world String, instead of returning 
     * "Hello, world!". A test case will check if the behavior is as expected.
     * You should use the test results to check for differences.
     * 
     * @return "Hello, world!"
     */
    public String sayHello(){
        return "Hello, world!";
    }
}